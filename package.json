{
  "name": "telegram-trading-bot",
  "version": "1.0.0",
  "description": "A Telegram bot for trading stocks with a shared global account",
  "main": "api/webhook.js",
  "scripts": {
    "start": "node api/webhook.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
